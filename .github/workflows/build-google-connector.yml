name: Build Google Connector

on:
  push:
    branches: [main, master]
    tags: ['v*']
    paths:
      - 'connectors/google/**'
      - 'shared/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/build-google-connector.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'connectors/google/**'
      - 'shared/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/build-google-connector.yml'

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/build-connector.yml
    with:
      connector-name: google
      connector-type: rust
