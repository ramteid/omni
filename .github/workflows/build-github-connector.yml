name: Build GitHub Connector

on:
  push:
    branches: [main, master]
    tags: ['v*']
    paths:
      - 'connectors/github/**'
      - 'sdk/python/**'
      - '.github/workflows/build-github-connector.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'connectors/github/**'
      - 'sdk/python/**'
      - '.github/workflows/build-github-connector.yml'

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/build-connector.yml
    with:
      connector-name: github
      connector-type: python
