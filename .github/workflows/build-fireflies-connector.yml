name: Build Fireflies Connector

on:
  push:
    branches: [main, master]
    tags: ['v*']
    paths:
      - 'connectors/fireflies/**'
      - 'shared/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/build-fireflies-connector.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'connectors/fireflies/**'
      - 'shared/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/build-fireflies-connector.yml'

jobs:
  build:
    uses: ./.github/workflows/build-connector.yml
    with:
      connector-name: fireflies
      connector-type: rust
