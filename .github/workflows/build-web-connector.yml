name: Build Web Connector

on:
  push:
    branches: [main, master]
    tags: ['v*']
    paths:
      - 'connectors/web/**'
      - 'shared/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/build-web-connector.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'connectors/web/**'
      - 'shared/**'
      - 'Cargo.toml'
      - 'Cargo.lock'
      - '.github/workflows/build-web-connector.yml'

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/build-connector.yml
    with:
      connector-name: web
      connector-type: rust
