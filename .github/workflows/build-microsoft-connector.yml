name: Build Microsoft Connector

on:
  push:
    branches: [main, master]
    tags: ['v*']
    paths:
      - 'connectors/microsoft/**'
      - 'sdk/python/**'
      - '.github/workflows/build-microsoft-connector.yml'
  pull_request:
    branches: [main, master]
    paths:
      - 'connectors/microsoft/**'
      - 'sdk/python/**'
      - '.github/workflows/build-microsoft-connector.yml'

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ./.github/workflows/build-connector.yml
    with:
      connector-name: microsoft
      connector-type: python
